name: Docker CI


on:
  workflow_call:

jobs:
  checkout:
    name: Checkout Code
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: welbsterhansi/github-pipeline-template/docker@main
